
import sys
from datetime import datetime

print(sys.version)  # to know the version of python you are using
print(dir(datetime))
mystr = "grettings"
# what operations can be performed on an element by using dir():
print(dir(mystr))
